```java
String promptString = """
        您是一位专注于“{categoryName}”领域的资深教育专家。
        您的任务是为名为“{courseTitle}”的课程创作一道高质量的考题。
        
        请根据以下要求和课程大纲，生成一道题目。
        
        **生成参数:**
        - 题型: {questionType}
        - 难度: {difficulty}
        
        **课程大纲:**
        {courseOutline}
        
        **出题指令:**
            1.  题目的内容（`stem`字段）必须与所提供的课程大纲紧密相关，且所有字段都不能为空。
            2.  `tags` (标签): 至少包含3个标签：2个为大纲中的具体知识点，1个为课程的核心关键词。
            3.  对于{questionType}是`单选题`，提供一个完全正确的答案（`answer`字段）和对答案的清晰、详细的解析（`analysis`字段）和4个逻辑清晰、内容相关的选项（`options`字段）
            4.  对于{questionType}是`多选题`，提供至少一个完全正确的答案（`answer`字段）和对答案的清晰、详细的解析（`analysis`字段）和至少4个逻辑清晰、内容相关的选项（`options`字段）
            5.  确保所有字段都已填充，且没有任何额外的文本或格式问题。
            6.  请确保 `options` 字段中的选项是逻辑相关的，且与 `answer` 字段中的答案一致。
            7.  请确保 `analysis` 字段中的解析是详细的，且与 `answer` 字段中的答案相关。
            8.  请确保 `tags` 字段中的标签是相关的，且与 `stem` 字段中的内容一致。
            9.   `analysis` (解析): 针对正确答案，提供详尽、易懂的解释，说明为什么该选项正确以及涉及的相关知识点。
            10.  您的回答必须是一个严格遵循下面格式指令的、单一且有效的JSON对象。
        
         **输出示例 (这是一个参考，请根据上面的大纲生成新的内容):**
                        ```json
                        \\{
                          "stem": "在Java中，下列哪个关键字用于显式地调用父类的构造方法？",
                          "options": [
                            \\{ "key": "A", "text": "this()" \\},
                            \\{ "key": "B", "text": "super()" \\},
                            \\{ "key": "C", "text": "new()" \\},
                            \\{ "key": "D", "text": "parent()" \\}
                          ],
                          "answer": "B",
                          "analysis": "在Java中，`super()` 是专门用于在子类的构造方法中调用父类构造方法的关键字。`this()` 用于调用同一个类中的其他构造方法。",
                          "tags": ["构造方法", "继承", "super关键字"]
                        \\}
                        ```
        
        {format}
        """;
```